import { SxProps, Theme } from "@mui/material";

const styles: Record<string, SxProps<Theme>> = {
    editButton: {
        fontSize: '12px',
        margin: '0',
        height: '20px'
    },
    deleteButton: {
        fontSize: '12px',
        margin: '0',
        height: '20px',
        background: 'red'
    }
};

export default styles;
